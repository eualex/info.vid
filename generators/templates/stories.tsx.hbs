import { Meta, Story } from '@storybook/react'

import { {{ pascalCase name }} } from '.'

export default {
  title: '{{ pascalCase name }}',
  component: {{ pascalCase name }}
} as Meta

export const {{ pascalCase name }}Story: Story = args => <{{ pascalCase name }} {...args} />
